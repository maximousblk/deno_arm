language: rust
rust: stable
cache: cargo
os: linux
dist: bionic
arch: arm64
env:
  DENO_CANARY: true

script:
  - git clone https://github.com/denoland/deno.git
  - cd deno
  - cargo build --release --locked
